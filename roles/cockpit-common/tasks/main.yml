---
- name: Install packages needed for all nodes 
  yum:
    name: "{{ packages }}"
    state: latest
  vars:
    packages:
    - cockpit
    - cockpit-bridge
    - cockpit-packagekit
    - cockpit-ws
    - cockpit-composer
    - cockpit-pcp
    - cockpit-session-recording
    - cockpit-machines
    - subscription-manager-cockpit
    - cockpit-system
    - cockpit-storaged
